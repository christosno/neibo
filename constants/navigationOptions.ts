import { theme } from "@/theme";

export const defaultScreenOptions = {
  contentStyle: { backgroundColor: theme.colors.slateDark },
  headerStyle: { backgroundColor: theme.colors.slateDark },
  headerTintColor: theme.colors.slateLight,
  headerTitleStyle: { color: theme.colors.slateLight },
};

export const defaultTabBarOptions = {
  tabBarStyle: { backgroundColor: theme.colors.slateDark },
  tabBarActiveTintColor: theme.colors.slateLight,
  tabBarInactiveTintColor: theme.colors.slateLight,
};
